#include<iostream>
using namespace std;
template <class type>
type find(type a, type b)
{
	if (a>b)
	return a
	else
	return b
	
}
int main()
{int a,b;
	cout<<"enter any two numbers";
	cin>>a>>b;
	cout<<"the max number is"<<max(a,b);
}
